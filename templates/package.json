{
  "name": "<%= name %>",
  "description": "",
  "version": "0.0.0",
  "devDependencies": {
    "bower": "^1.3.8",
    "browser-sync": "^1.1.2",
    "coffee-script": "^1.7.1",
    "del": "^0.1.0",
    "gulp": "^3.8.6",
    "gulp-cache": "^0.2.0",
    "gulp-changed": "^0.4.0",
    "gulp-coffee": "^2.1.1",
    "gulp-coffeelint": "^0.3.2",<% if (includeCompass) { %>
    "gulp-compass": "^1.1.9",<% } %><% if (includeSass) { %>
    "gulp-ruby-sass": "^0.5.0",<% } %><% if (!includeCss) { %>
    "gulp-plumber": "^0.6.3",<% } %>
    "gulp-htmlmin": "^0.1.3",
    "gulp-if": "^1.2.4",
    "gulp-imagemin": "^0.6.1",
    "gulp-jshint": "^1.8.0",
    "gulp-load-plugins": "^0.5.2",
    "gulp-minify-css": "^0.3.7",
    "gulp-mocha": "^0.5.1",
    "gulp-rename": "^1.2.0",
    "gulp-replace": "^0.4.0",
    "gulp-size": "^0.4.0",
    "gulp-uglify": "^0.3.1",<% if (!includeRequireJS) { %>
    "gulp-useref": "^0.5.0",<% } %>
    "gulp-util": "^3.0.0",
    "lazypipe": "^0.2.2",
    "mocha": "^1.21.0",<% if (includeRequireJS) { %>
    "requirejs": "^2.1.14",<% } %>
    "run-sequence": "^0.3.6",
    "uuid": "~1.4.1"
  },
  "scripts": {
    "start": "gulp",
    "test": "bower install && gulp test_coffee && gulp release --env production && gulp test"
  },
  "engines": {
    "node": ">= 0.10.0"
  }
}
