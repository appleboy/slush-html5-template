{
  "name": "<%= name %>",
  "description": "",
  "version": "0.0.0",
  "devDependencies": {
    "bower": "^1.3.12",
    "browser-sync": "^1.5.7",
    "coffee-script": "^1.8.0",
    "del": "^0.1.3",
    "gulp": "^3.8.8",
    "gulp-cache": "^0.2.4",
    "gulp-changed": "^1.0.0",
    "gulp-coffee": "^2.2.0",
    "gulp-coffeelint": "^0.4.0",<% if (includeCompass) { %>
    "gulp-compass": "^1.3.2",<% } %><% if (includeSass) { %>
    "gulp-ruby-sass": "^0.5.0",<% } %>
    "gulp-csso": "^0.2.9",
    "gulp-if": "^1.2.5",
    "gulp-imagemin": "^1.0.1",
    "gulp-jshint": "^1.8.5",
    "gulp-load-plugins": "^0.7.0",
    "gulp-minify-html": "^0.1.6",
    "gulp-mocha": "^1.1.1",
    "gulp-plumber": "^0.6.6",
    "gulp-rename": "^1.2.0",
    "gulp-replace": "^0.4.0",
    "gulp-size": "^1.1.0",
    "gulp-uglify": "^1.0.1",<% if (!includeRequireJS) { %>
    "gulp-useref": "^0.5.0",<% } %>
    "gulp-util": "^3.0.0",
    "lazypipe": "^0.2.2",
    "mocha": "^1.21.4",
    "require-dir": "^0.1.0",<% if (includeRequireJS) { %>
    "requirejs": "^2.1.15",<% } %>
    "run-sequence": "^1.0.1",
    "uuid": "^2.0.1"
  },
  "scripts": {
    "start": "gulp",
    "release": "bower install && gulp release --env production",
    "test": "bower install && gulp test_coffee && gulp release --env production && gulp test"
  },
  "engines": {
    "node": ">= 0.10.0"
  }
}
